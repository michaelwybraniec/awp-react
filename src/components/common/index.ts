/**
 * Common Components
 * Reusable components used across the application
 */

// This file will export common components as they are created
// Example: export { LoadingSpinner } from './loading-spinner';
// Example: export { ErrorBoundary } from './error-boundary';
// Example: export { Modal } from './modal';

// Temporary empty export to make this a valid module
export {};
